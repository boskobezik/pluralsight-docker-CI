version: 2.1
jobs:
  build:
    docker:
      - image: 'circleci/node:latest'
    steps:
      - checkout
      - run: npm install; node ./test/test.js
deployment:
  dockerhub:
    branch: master
    commands:
    - $DOCKER_HUB_BUILD_TRIGGER
test:
  override:    
    - mocha

